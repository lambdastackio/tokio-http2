(function() {var implementors = {};
implementors["tokio_http2"] = ["impl FromPrimitive for <a class='enum' href='https://lifthrasiir.github.io/rust-chrono/chrono/enum.Weekday.html' title='chrono::Weekday'>Weekday</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
